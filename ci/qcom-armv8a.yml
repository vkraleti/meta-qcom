# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json

header:
  version: 14
  includes:
  - ci/base.yml

local_conf_header:
  external_devicetree: |
    PREFERRED_PROVIDER_virtual/dtb = "devicetree-dummy"
    QCOM_BOOTIMG_DEVICETREE = "qcom-armv8-dummy.dtb"

machine: qcom-armv8a
